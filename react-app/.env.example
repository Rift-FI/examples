# Test project API key — points at a sandbox Rift project that anyone
# poking around the examples can use. Replace with your own sk_… from
# https://service.riftfi.xyz once you've created your project.
#
# Vite reads .env.local (gitignored) at runtime, not .env.example. To
# run this example: `cp .env.example .env.local && npm install && npm run dev`
VITE_RIFT_API_KEY=sk_c8d7717ff9abf62344be5e2f8f5200daab1cf0409f4aceb7239de25b088e369f
